# toot.community Argo CD

This is the Argo CD configuration for toot.community.

## gRPC
You can authenticate to Argo CD from CLI using:

```shell
$ argocd login argocd.toot.community --grpc-web --sso
```
